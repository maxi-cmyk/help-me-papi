# Hackathon Inbox

Optional local inbox for hackathon-only notes.

Rules:
- Put each tagged note in its own paragraph or bullet block.
- Use `#path/to/file` style tags that point to an existing repo destination.
- If a tag does not resolve to a file, the block stays here.
